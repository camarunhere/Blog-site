
// In browser the window is the global object but in node our global object is not window but global and that represents the global context in a node environment
// Global Object

//console.log(global);

// setTimeout(() => {
//     console.log('in the timeout'); //Runs only once
//     clearInterval(int);
// }, 3000);

// const int = setInterval(() => {
//     console.log('in the interval');//Runs after every second...
// }, 1000);

// console.log(__dirname);   //filepath
// console.log(__filename);  //filepath to the file

console.log(document.querySelector);





